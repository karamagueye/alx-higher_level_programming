#!/usr/bin/python3
"""Define"""
def inherits_from(obj, a_class):
    """Check
    Args:
    Return:
    """
    if issubclass(type(obj).a_class) and type(obj) != a_class:
        return False
    return False
